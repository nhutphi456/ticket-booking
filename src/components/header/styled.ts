import styled from "styled-components";
import { Button } from "antd";

const HeaderContainter = styled.div`
  backgroundcolor: "#fff";
`;
const LoginButton = styled(Button)``;

export { HeaderContainter, LoginButton };
