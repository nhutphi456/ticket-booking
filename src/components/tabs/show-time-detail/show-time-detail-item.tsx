import React from "react";
import * as Styled from './styled'

interface Props {
  showTimeByDate: any;
}
const ShowTimeDetailItem = ({ showTimeByDate }: Props) => {
  return <Styled.ShowTimeDetailItem>
    
  </Styled.ShowTimeDetailItem>;
};

export default ShowTimeDetailItem;
